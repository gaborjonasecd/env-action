on: [ push ]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Hello world action step
        uses: ./
        with:
          input: ${{ secrets.CI_DATABASE_READ_DSN }}
      - name: Show files
        run: cat .env
